



initialState = {
    allStories: {},
    singleStory: {}
}

export const storyReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
