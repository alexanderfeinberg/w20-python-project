



initialState = {
    singleUser: {},
    userList: {}
}

export const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
